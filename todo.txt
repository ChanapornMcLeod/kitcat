# KitCat LINE Bot — TODO

## 🔌 NEXT: plug in the real TMS API (when given)

- [ ] Fill in `.env`: `TMS_BASE` + `TMS_API_TOKEN` (removes MOCK mode)
- [ ] Confirm/adjust in `tms.js`:
      - `TMS_PATHS` — the 6 endpoint paths (api1..api6)
      - HTTP method per op (currently all POST)
      - request body field names (see payload contracts below)
      - `normalizeList()` / mapSchedule/mapVehicle/mapType — response shapes
- [ ] Decide: does TMS return schedule list keyed by LINE userId or driverRef? (we send both)

### Payload contracts our bot sends (adjust if TMS differs)
| op | body |
|---|---|
| api1 listMaintenanceSchedules | `{ driverRef, lineUserId }` → list `{id,label,sub}` |
| api2 submitMaintenance | `{ driverRef, lineUserId, scheduleId, mileage, shopName, selfService }` |
| api3 listVehicles | `{ driverRef, lineUserId }` |
| api4 listMaintenanceTypes | `{ vehicleId, driverRef, lineUserId }` |
| api5 createMaintenance | `{ driverRef, lineUserId, vehicleId, maintenanceTypeId, mileage, shopName, selfService }` |
| api6 registerDriver | `{ lineUserId, driverRef }` |

## ⚡ Quick test on phone (MOCK mode, live now)

- [ ] Register: tap 🪪 REGISTER → type e.g. `DRV-0042` → linked
- [ ] Submit: tap 🔧 SUBMIT → pick schedule → mileage → self-service? → shop → confirm
- [ ] No schedules case → vehicle → type → mileage → shop/self-service → confirm (api5 create)
- [ ] Send photo mid-flow → attached as POD

## 🖥 Machine setup (your Terminal — sandbox blocked these)

- [ ] Auto-start at login:
      ```bash
      mkdir -p ~/Library/LaunchAgents ~/Dev/aiProjects/kitcatLine/logs
      cp ~/Dev/aiProjects/kitcatLine/deploy/launchd/com.kitcat.linebot.plist ~/Library/LaunchAgents/
      launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.kitcat.linebot.plist
      ```
- [ ] `cd ~/Dev/aiProjects/kitcatLine && git push`

## 🔐 Hygiene / later

- [ ] Rotate LINE Channel Access Token (was pasted in chat)
- [ ] POD photo upload to TMS: currently metadata-only forward + local save; add real upload (multipart / presigned URL) when API given
- [ ] URL stability: ngrok static domain or named tunnel (trycloudflare rotates; watchdog auto-re-registers)
- [ ] Session store → Redis/DB if multi-instance ever needed

## ✅ DONE

- [x] PoC: webhook, signature verify, rich menu, flex cards, dynamic per-user menu
- [x] **Functionality1**: submit-maintenance flow (api1→api2; fallback api3→api4→api5) — e2e tested, both paths
- [x] **Functionality2**: register driver (driverRef input → api6) + local link store
- [x] Rich menu v2: 🔧 SUBMIT / 🪪 REGISTER / 🚗 STATUS tiles
- [x] TMS adapter layer with MOCK mode (all 6 ops)
- [x] Self-healing launcher (watchdog + auto webhook re-registration)
- [x] GitHub: https://github.com/ChanapornMcLeod/kitcat

## ℹ️ Current live state

- Webhook: `https://spin-funny-town-dedicated.trycloudflare.com/webhook` (active, MOCK TMS)
- Modules: `server.js` (flows) · `tms.js` (API layer) · `flex.js` (cards) · `session.js` (state)
- Rich menu bar is **mobile-app only** (Mac LINE never shows it; taps still work)
