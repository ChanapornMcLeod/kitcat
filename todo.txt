# KitCat LINE Bot — TODO

## ⚡ DO FIRST (bot won't receive messages without these)

- [ ] **Activate webhook in LINE Developers console** ← root cause of "no response" right now
      https://developers.line.biz/console/ → channel 2011484173 → Messaging API tab →
      Webhook URL: paste the CURRENT tunnel URL below → press **Verify** → set **Use webhook: ON**
      Current URL: https://holdings-preservation-pizza-standards.trycloudflare.com/webhook
      (URL changes on every restart — check `logs/bot.log` for the latest, or run `./start.sh`)
- [ ] Turn **Auto-reply messages: OFF** (Messaging API tab, so LINE's canned replies don't compete with the bot)
- [ ] Add bot `@967tyqzr` as friend → send "menu" → expect Flex button card 🐱

## 🔁 URL stability (recommended soon)

- [ ] **ngrok static domain** (free account): sign up → `ngrok config add-authtoken <token>` →
      `ngrok http 3000` gives a permanent URL → verify ONCE in console, never again.
      (trycloudflare URLs rotate on every tunnel restart = re-Verify in console each time)

## 🖥 Machine setup (do in your own Terminal — sandbox can't touch these)

- [ ] **Auto-start bot at login**:
      ```bash
      mkdir -p ~/Library/LaunchAgents ~/Dev/aiProjects/kitcatLine/logs
      cp ~/Dev/aiProjects/kitcatLine/deploy/launchd/com.kitcat.linebot.plist ~/Library/LaunchAgents/
      launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.kitcat.linebot.plist
      ```
- [ ] **Push pending commit** (sandbox can't read ~/.ssh keys):
      ```bash
      cd ~/Dev/aiProjects/kitcatLine && git push
      ```

## 🔐 Hygiene

- [ ] Rotate the LINE Channel Access Token (it was pasted in chat) — console → Issue new long-lived token → update `.env`
- [ ] Keep `.env` out of git (already gitignored — verify before any manual adds)

## 🚀 Later / production

- [ ] Move off trycloudflare (no uptime guarantee): cheapest stable option = cheap domain + Cloudflare DNS + named tunnel, or deploy to a VPS
- [ ] Decide message-handling requirements (what should each button do? real business flows?)
- [ ] Add rich menus (LINE native bottom-bar menu) once requirements are set
- [ ] Persist chat history / user state if needed

## ℹ️ Status (2026-09-08)

- Server + tunnel running: `logs/bot.log`; single-instance verified
- Webhook endpoint registered + reachable (LINE platform test passes) but **inactive until console Verify** ← do first task above
- GitHub: https://github.com/ChanapornMcLeod/kitcat
