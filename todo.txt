# KitCat LINE Bot — TODO

## ⚡ DO NOW

- [ ] **Test on phone**: open KitCat chat → tap the bottom **KitCat Menu** bar
      (✅ Service done / 📷 Send photo / 🚗 My status / 🌐 Open app) and send a photo
- [ ] **Push commits** (sandbox can't read ~/.ssh): `cd ~/Dev/aiProjects/kitcatLine && git push`

## 🔌 Web app integration (the real work — waiting on YOU)

- [ ] Provide **WEBAPP_URL** + **WEBAPP_API_TOKEN** → set in `.env`, restart bot
- [ ] Agree on endpoints our bot will call (proposed):
      - `POST /api/line/service-completions` — driver tapped "Service done" `{driverLineUserId, reportedAt, note}`
      - `POST /api/line/driver-status`      — driver asked status `{driverLineUserId}` → app returns short text
      - `POST /api/line/driver-photos`      — photo metadata (file saved locally in `uploads/`, multipart upload optional)
- [ ] Decide auth for webapp endpoints (shared bearer token / API key / JWT)
- [ ] Decide how staff see reports (existing web app pages? LINE notifications to staff too?)

## 🔁 URL stability (later, as agreed)

- [ ] ngrok static domain OR Cloudflare named tunnel + your own domain
      ⚠️ Until then: every restart changes the tunnel URL — `./start.sh` re-registers
      automatically, but the old verified URL dies with it (webhook needs no re-Verify
      since registration re-activates it — keep bot running)

## 🖥 Machine setup (your Terminal — sandbox blocked these)

- [ ] Auto-start at login:
      ```bash
      mkdir -p ~/Library/LaunchAgents ~/Dev/aiProjects/kitcatLine/logs
      cp ~/Dev/aiProjects/kitcatLine/deploy/launchd/com.kitcat.linebot.plist ~/Library/LaunchAgents/
      launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.kitcat.linebot.plist
      ```

## 🔐 Hygiene

- [ ] Rotate the LINE Channel Access Token (pasted in chat earlier) — console → Issue new → update `.env`
- [ ] Keep `.env` out of git (gitignored — verify before manual adds)

## 🚀 Later

- [ ] Persist photo uploads to your storage (S3/OSS/NAS) instead of local `uploads/`
- [ ] Rich menu v2 when requirements firm up (per-role menus? more tiles?)
- [ ] Staff-side notifications in LINE when drivers submit reports
- [ ] Deploy to a VPS / server for uptime

## ✅ DONE (2026-09-08)

- [x] Webhook live & active; messages, flex menu, quick replies working
- [x] Rich menu bar "KitCat Menu" attached to ALL chats (4 actions)
- [x] Action dispatcher + mock webapp layer (`callWebApp`) — add features by adding one handler
- [x] Photo capture: images saved to `uploads/` + forwarded (mock) to web app
- [x] git repo pushed: https://github.com/ChanapornMcLeod/kitcat
